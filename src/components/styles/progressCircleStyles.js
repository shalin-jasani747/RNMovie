import Colors from '../../theme/Colors';
import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  percentageCirclePosition: {
    position: 'absolute',
    bottom: 0,
    marginHorizontal: 5,
    marginBottom: -1 * 10
  },
  percentageText: { color: Colors.white }
});

export default styles;
